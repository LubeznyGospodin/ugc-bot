# UGC RADAR bot — карта проекта (агент-лог)

Дата старта ревизии: 2026-07-04. Модель: Opus 4.8 (предыдущая сессия шла на Haiku и застряла).

## Верхняя цель
Рабочий Telegram-бот регистрации UGC-креаторов (UGC RADAR): анкета проходится без
зависаний, чат чистый с автоскроллом, данные попадают в актуальную Google-таблицу,
раздел «Запросы брендов» без мусорных кнопок. Эталон UX: @bro_hit_bot.

## Репозиторий и деплой
- Рабочий репо: `popovigory/ugc-bot-v2` (origin). НЕ `LubeznyGospodin/ugc-bot` — туда
  ошибочно пушила старая сессия. Это тупик, туда не деплоим.
- Локально: `~/Desktop/ugc_bot_v2`, ветка master, HEAD=e1c59c7.
- Railway проект: "spectacular-art" (из DEPLOY.md). Деплой: `railway up` или GitHub push.
- Env на Railway: BOT_TOKEN, ADMIN_IDS, DATABASE_URL, SHEETS_WEBHOOK_URL, SHEETS_WEBHOOK_SECRET.
- Стек: aiogram 3, polling, MemoryStorage (FSM в памяти → перезапуск сбрасывает анкеты).

## Ключевой вывод
Фиксы для багов 1 и 4 УЖЕ написаны локально (uncommitted), но не закоммичены и не
задеплоены → прод крутит старый коммит и «всё ещё не работает». Паттерн: «код изменён» ≠
«результат доказан live».

## Баги (из ТЗ Игоря)
1. Зависание после ввода номера. КОРЕНЬ: в задеплоенной версии после phone вызывается
   `STEP_PROMPTS[Registration.categories]` — ключа нет → KeyError → бот молчит.
   Локальный фикс (ветка `if nxt == Registration.categories → _show_categories`) корректен,
   но не выкачен. Также requirements: добавлен greenlet (без него падал init БД).
2. Нет автоскролла после удаления сообщения (десктоп). Локальный «фикс» через zero-width
   спейсер ненадёжен (Telegram отклоняет пустые сообщения). Нужен нормальный подход —
   сверить с @bro_hit_bot (вероятно edit-in-place вместо delete+send).
3. Синхронизация с Google Sheets «кривая». Новая целевая таблица:
   1Hoqi-lLxyJtApNR6WGN59Rwz0u0hLPqFuDE-fQbzYyI (gid 1174202351).
   Синк идёт через Apps Script webhook (SHEETS_WEBHOOK_URL). Логика записи — на стороне
   Apps Script/таблицы, не в этом репо. Надо проверить: к какой таблице привязан скрипт,
   куда реально пишет register/update/lookup, совпадают ли колонки.
4. Кнопка «Пока пусто, загляните позже» в «Запросы брендов». Локально уже удалена
   (keyboards.py + brands.py brand:none), не выкачено.

## Критерии готовности (live-приёмка)
- [ ] Регистрация проходит от /start до «Готово» без зависаний (глазами в Telegram).
- [ ] После каждого шага чат скроллится к новому сообщению (десктоп).
- [ ] После submit в целевой таблице появляется/обновляется строка (глазами в Sheet).
- [ ] «Запросы брендов» при пустом списке — только текст, без кнопки-заглушки.
- [ ] Задеплоено на Railway; live hash до/после зафиксирован.

## Что НЕ трогать без нужды
- Не пушить в LubeznyGospodin/ugc-bot.
- Не ломать дедуп-логику (lookup по Sheet — источник правды).
- Не выкатывать старую локальную копию поверх более свежей live (сверить перед деплоем).

## Live-находки (2026-07-04)
- Railway: проект spectacular-art, сервис Online (~150MB RAM). Деплой ТОЛЬКО через
  `railway up` по CLI (не GitHub). Активный деплой 8ч назад via CLI. Env: 5 переменных
  (BOT_TOKEN, ADMIN_IDS, DATABASE_URL, SHEETS_WEBHOOK_SECRET, SHEETS_WEBHOOK_URL).
  SHEETS_WEBHOOK_URL = script.google.com/macros/s/AKfy… (Apps Script web app).
- Эталон @bro_hit_bot ("Бро.Хит"): НЕ удаляет сообщения, шлёт новые подряд → нативный
  автоскролл работает. Вывод для бага 2: удаление ломает скролл; надо либо edit-in-place,
  либо перестать удалять (как эталон).
- Целевая таблица 1Hoqi… = ответы Google-формы, 90+ реальных анкет (24.05–01.06), 23
  колонки. У ВСЕХ строк «Есть в боте»=нет, «Chat ID» пусто → бот с ней НЕ синхронизирован.
  Схема бота (10 полей) уже, чем форма (23 колонки). Баг 3 = переключить Apps Script на эту
  таблицу + маппинг колонок + решить семантику (append/update/отдельная вкладка).

## РАЗВИЛКИ ДЛЯ ИГОРЯ (заданы вопросом)
1. Метод деплоя на Railway (CLI-only): создать deploy-токен и катать самому / подключить
   GitHub автодеплой / Игорь катит сам по команде.
2. Семантика бага 3: как бот пишет в боевую форму-таблицу (обновлять существующую строку по
   @tg/имени + пометка «Есть в боте»=да, Chat ID / отдельная вкладка / полный маппинг).

## План после ответов
- Багы 1 и 4: фиксы готовы локально, коммит + deploy.
- Баг 2: реализовать нормальный автоскролл (edit-in-place или append-режим как эталон).
- Баг 3: по выбранной семантике переключить Apps Script на таблицу 1Hoqi + маппинг, live-тест.

## Сделано в коде (закоммичено локально, commit "fix: registration hang...")
- Баг 1: фикс phone->categories (ветка _show_categories) — проверен логикой (chain OK,
  нет KeyError). + greenlet в requirements (без него падал submit/upsert в async БД).
- Баг 2: chat_cleanup.render_screen переписан на edit-in-place (не delete+send) → нет
  «прыжка», скроллить не нужно. Fallback на delete+send если правка невозможна.
- Баг 4: убрана кнопка-заглушка «Пока пусто, загляните позже» (keyboards.py brands_list_keyboard).
- Смоук: py_compile OK, импорт всех модулей OK, FSM-цепочка OK.
- ВАЖНО (в бэклог, не в scope сейчас): FIELD_BY_STATE даёт data["telegram"], а в БД поле
  telegram_contact → username/telegram не сохраняется локально (профиль показывает «—»).

## БЛОКЕР ДЕПЛОЯ (зафиксировано)
- Railway деплоится только `railway up` (CLI). CLI в песочницу не ставится: бинарник
  редиректит на release-assets.githubusercontent.com → прокси = 000 (блок).
- git push из песочницы невозможен: нет кэш-кредов (GIT_TERMINAL_PROMPT=0 → fatal).
- GitHub-коннектора в реестре нет.
- Railway↔GitHub: «Connect Repo» доступен, но нужен код на GitHub (буду решать с Игорем).
- Вывод: автономно катнуть не могу без (а) одной команды Игоря `railway up`, либо
  (б) настройки GitHub-автодеплоя + один git push Игоря, либо (в) хрупкой правки файлов
  через веб-редактор GitHub. Спросил Игоря.

## Баг 3 — предложенный маппинг (bot → колонка таблицы 1Hoqi), ждёт ОК Игоря
- Имя и фамилия(B)←full_name; Возраст(C)←age; Телефон(D)←phone; Телеграм(E)←telegram;
  Адрес(F)←city; Соцсети(G)←instagram; Оплата(P)←rate; Другие соцсети(Q)←other_socials;
  Категория(R)←category; Портфолио(S)←portfolio; Источник(T)="Бот"; Есть в боте(U)="да";
  Chat ID(V)←chat_id; Обновлено(W)←now.
- Не собирается ботом (оставить пусто/не трогать при update): Рост(H), В электру(I),
  Офер(J), Размер одежды(K), Статус в базу(L), Актёрские(M), Фото(N), Параметры тела(O).
- На UPDATE существующей формы-строки трогаем только U/V/W (не перезаписываем кураторские
  колонки Статус/В электру и т.д.). Новый регистрант (нет в форме) → append новой строкой.
- Реализация: переписать Apps Script (Код.gs) под эту схему + привязать к 1Hoqi; doLookup_
  по колонкам E(Телеграм)/B(Имя). После ОК Игоря.

## LIVE-ПРОВЕРКА (2026-07-04, деплой 0237dc3f via railway up Игоря)
- Логи старта: "Бот запускается (polling)", "Run polling for bot @ugc_radarbot 'UGC RADAR'" — без ошибок.
- Баг 1 ✅ ДОКАЗАН: прошёл всю анкету в Telegram Web — name→contact→instagram→other→rate→
  portfolio→age→city→phone→КАТЕГОРИИ→confirm. После телефона показались категории (не завис,
  нет KeyError). Дошёл до «Проверь анкету» с кнопками. Submit НЕ жал (в базу не писал).
- Баг 2 ✅ механизм подтверждён: экран редактируется на месте, ответы юзера удаляются, delete+send
  убран. ВНИМАНИЕ: тестировал в Telegram WEB; визуальный автоскролл именно в ДЕСКТОП-приложении
  глазами не проверял (корень — delete+send — устранён структурно, скроллить больше нечему).
- Баг 4 ✅ ДОКАЗАН: сравнение в чате — 08:47 (старый деплой) под «Пока нет активных запросов»
  была кнопка «Пока пусто, загляните позже»; 09:45 (новый) кнопки нет. Убрана.
- Наблюдение (НЕ из 4 багов): при ОЧЕНЬ быстром вводе пачкой поле «Город» склеилось как
  «25ТестГород» (age+city). Похоже на гонку конкурентной обработки апдейтов aiogram при вводе
  быстрее ответа бота. Реальный юзер так быстро не печатает. В бэклог: сделать per-chat
  последовательную обработку (или это артефакт авто-теста).
- Тестовых данных в таблицу/БД НЕ создано (submit не нажимал).

## БАГ 3 — реальная диагностика (по таблице + Apps Script)
- Apps Script УЖЕ привязан к таблице 1Hoqi (лист "Ответы на форму (1)"), маппинг колонок
  A–T верный, register после append ставит Есть в боте(21)=да и Chat ID(22). Т.е. таблица и
  маппинг менять НЕ надо.
- Доказано live: строка 206 (02.07 = завершённая регистрация Игоря) = полные поля +
  Источник=Telegram-бот + Есть в боте=ДА. Вчера бот узнал Игоря ("Ты уже в базе") → lookup
  читает записанное. Полный круг register→lookup работает для ЗАВЕРШЁННОЙ регистрации.
- => Исходное "нет синхронизации" = следствие бага 1 (регистрация не доходила до submit).
- НО найдена настоящая "кривая логика": строки 207–235 (~29 шт, Jul 2–4) — Источник=
  Telegram-бот, но поля ПУСТЫЕ и "Есть в боте" ПУСТО. Бот плодит разреженные мусорные строки
  (мой /start в 10:01 создал пустую строку 235; submit я не жал). Значит append происходит не
  только на полном register — где-то на /start / lookup / partial. + lookup непоследователен
  (вчера узнал, сегодня "не нашёл") — возможно из-за мусорных дублей или бага doLookup_/doUpdateRow_.
- Точный триггер append надо добить чтением doLookup_/doUpdateRow_ (в фазе фикса).
- РИСК: правка production Apps Script + удаление ~29 строк = массовое изменение боевых данных →
  делаю только после ОК Игоря. Тестовые строки больше НЕ создаю.

## БАГ 3 — уточнённый диагноз (после структурного разбора Apps Script)
- Структура doPost: ветки lookup/update/stats/brands (строки 11-14) все с return; append только
  в register (строка 32), ставит да (34). doLookup_ (122) и doUpdateRow_ (162) — appendRow=0,
  но doUpdateRow_ делает setValue по номеру строки, включая setДА на строке 178.
- ЭМПИРИЧЕСКИ подтверждён активный триггер: каждый /start создаёт пустую строку (235=10:01,
  236=15:50 — мои два /start). Т.е. баг живой под текущим кодом.
- Гипотеза root cause: при узнавании /start→found→_finish_recognized→update_row(result.row)
  в doUpdateRow_ приходит неверный row (за пределами данных) → getRange(row,..).setValue
  расширяет лист → новая пустая строка внизу. + непоследовательный lookup (вчера узнал, сегодня нет).
- НЕ смог: прочитать текст doLookup_/doUpdateRow_ (content-фильтр блокирует любой вывод кода со
  script.google.com; структурные счётчики проходят, сам код — нет). Редактор Monaco править
  вслепую боевой вебхук = риск сломать регистрацию реальным креаторам → НЕ трогал.
- Сделан бэкап: лист "Ответы на форму (1) (копия)" (снимок всех 236 строк). Бот в него не пишет.
- Мусор НЕ удалял: без фикса триггера строки регенерируются на каждый /start (удаление было бы
  обманом "починил").
- Мои тестовые строки: 235, 236 (пустые, Игорь Попов) — мои артефакты, тоже мусор.

## БАГ 3 — ИСТИННЫЙ КОРЕНЬ (найден через журнал выполнений + управление развёртываниями)
- Все вызовы вебхука идут на "Версия 1" (закреплённое развёртывание). Deployment ID
  AKfycby... == SHEETS_WEBHOOK_URL бота. Все выполнения doPost — статус "завершено" (без ошибок).
- В @HEAD (редактор) doLookup_ read-only (append=0). Но live-вебхук = Версия 1, а не @HEAD.
- Логика: мой /start создаёт пустую строку, при этом live=Версия1; @HEAD lookup строку бы не
  создал → значит Версия 1 != @HEAD. Live крутит СТАРУЮ версию, где на lookup дописывается
  пустая лид-строка. Исправления (lookup read-only, простановка да/ChatID) есть в @HEAD, но
  их не передеплоили.
- ФИКС (не костыль): передеплоить текущий код редактора как новую версию (Управление
  развёртываниями → карандаш → Версия: Новая версия → Развернуть). Откат: вернуть Версию 1.
- РИСК: полностью тело doLookup_/doUpdateRow_ в @HEAD прочитать не смог (content-фильтр), но
  структура когерентна (register читается визуально целиком и корректен). После деплоя сразу
  тест: /start незнакомца не создаёт строку + реальная регистрация пишет 1 чистую строку с да.
- Guard "!data.phone" ОТКЛОНЁН как костыль (Игорь верно указал: телефон обязателен, значит
  пустышки не из нормального флоу — корень в устаревшем деплое).

## БАГ 3 — РЕШЕНО (2026-07-04 ~16:17)
- Передеплоил Apps Script: Управление развёртываниями → Новая версия → Версия 2 (URL и
  Deployment ID те же, бот не трогали). Одобрено Игорем.
- LIVE-проверка: после передеплоя /start НЕ создал новую строку (осталась 206; до этого каждый
  /start плодил строку 235/236). Триггер устранён.
- Чистка: удалены пустые мусорные строки 207–236 (30 шт). Последняя строка = 206 (реальная
  анкета Игоря: полные данные + Есть в боте=да). Все настоящие данные (формы 2–205 + бот 206) целы.
- Бэкап-лист "Ответы на форму (1) (копия)" оставлен как снимок (Игорь может удалить).
- FUZZY-ПОИСК/УЗНАВАНИЕ: ТОЖЕ починен передеплоем. Пост-деплой /start дал "✅Нашёл! Рад видеть
  снова → Игорь Попов, @LubeznyGospodin, Instagram ппп" — узнал корректно. Ранее непоследовательность
  была симптомом устаревшей Версии 1 (её же баганый lookup). Версия 2 = read-only doLookup_ →
  узнавание работает. При узнавании обновляет строку 206 (не плодит новую). U206=да подтверждено.
- Append-путь новой регистрации: доказан строкой 206 (полные данные + да). Свежий NEW-user append
  не гонял: аккаунт Игоря теперь узнаётся (не new), а правка его профиля затёрла бы реальные данные.
- Откат при необходимости: Управление развёртываниями → Версия 1.

## Итог по всем 4 багам
- Баг 1 (зависание): ФИКС + деплой (railway up) + live-проверка. ГОТОВО.
- Баг 2 (автоскролл): edit-in-place + деплой + проверено в web. ГОТОВО (десктоп-скролл глазами не смотрел).
- Баг 3 (sheets): корень = устаревшая Версия 1 вебхука; передеплой Версия 2 + чистка + live. ГОТОВО.
- Баг 4 (кнопка): ФИКС + деплой + live-проверка. ГОТОВО.

## НОВЫЙ БАТЧ (2026-07-04, автоскролл/меню/бренды)
- Автоскролл: render_screen переписан на send-first-delete-after (шлём новое ПЕРЕД удалением
  старого экрана → Telegram нативно скроллит вниз, как @bro_hit_bot). Файл chat_cleanup.py.
- Меню-спам: убраны все send_persistent_menu ("Меню всегда под рукой"). Reply-меню ставится
  один раз на первом экране /start (chat-level, остаётся висеть). start.py + registration.py.
- Фейковые бренды: добавлены на вкладку "Бренды" (br1 Бьюти/да, br2 Фитнес/спорт/да, br3 Еда/да,
  br4 Тревел/да, br5 Лайфстайл/нет). LIVE-проверено: бот показывает 4 активных, br5 (нет) скрыт,
  карточка+Откликнуться работают. (Мелочь: в описании br1 потерялось "Ищем " — косметика ввода.)
- Отклик на бренд (регистрация): бот brand_apply теперь пишет отклик через sheets_client.apply()
  на вкладку "Отклики" (создаётся автоматически) + уведомляет админов. Apps Script: добавлен
  doApply_ + маршрут action=apply, ПЕРЕДЕПЛОЕН как Версия 3 (URL/ID те же).
- ВНИМАНИЕ: при передеплое случайно откатился на Версию 1 на минуту — исправлено, сейчас
  Версия 3 (корректная, с doApply_ и всеми фиксами). Откат: Управление развёртываниями → любая версия.
- Смоук бота: py_compile + импорт всех модулей OK, sheets_client.apply present.
- ОСТАЛОСЬ: railway up для бота (правки автоскролл/меню/brand_apply не задеплоены). После —
  проверить: скролл в десктопе, нет спама меню, отклик пишется в "Отклики".

## LIVE-ПРОВЕРКА НОВОГО БАТЧА (2026-07-04, после railway up Игоря)
- Railway: ACTIVE "railway up ... Deployment successful" — новый код бота на проде.
- Меню-спам ✅ убран: новый /start (22:19) НЕ дал "Меню всегда под рукой" (прошлые /start слали).
- Автоскролл ✅: новые экраны аппендятся вниз (нативный скролл, как эталон). Десктоп — за Игорем глазами.
- Отклик на бренд ✅ ЗАПИСАН live: вкладка "Отклики" создана автоматически, строка
  [04.07 22:2x, br3, TastyBox Доставка, Игорь Попов, @LubeznyGospodin, 357892821]. doApply_ (V3) работает.
- Тестовая строка отклика (Игорь Попов/br3) оставлена — реальная запись, не мусор.

## БАТЧ 4 ПРАВОК (2026-07-04, вечер)
- #4 БАГ (при узнавании пропадают кнопки/всё удаляется): reply-меню было привязано к экрану
  "Ищу тебя", который удаляется при смене экрана → пропадало меню. Фикс: меню = отдельное
  сообщение-якорь (chat_cleanup.ensure_menu, _MENU_SET), НЕ в цепочке render_screen. Один раз на чат.
- #3 анимация поиска: _run_lookup_animation — 🔎↔🔍 + бегущие точки, sleep 0.35.
- #2 Telegram авто + телефон контактом: убрал шаг telegram_contact (берём @username в step_full_name),
  телефон — contact_request_keyboard (request_contact=True) + handlers phone_via_contact/phone_via_text,
  после телефона восстанавливаю меню (reset_menu+ensure_menu). keyboards.contact_request_keyboard.
- #1 правка анкеты: profile:edit → меню полей (edit_fields_keyboard, EDITABLE_FIELDS) → EditField.waiting_value
  → правим одно поле → update_row({field}) (doUpdateRow_ мапит поля, проверено) + upsert_creator → назад в меню.
- Смоук: py_compile + импорт OK, все функции/состояния на месте, telegram_contact убран из STEP_ORDER.
- Замечание: меню-сообщение "📋 Меню — кнопки снизу 👇" появляется 1 раз на чат (+1 раз после
  телефона при регистрации — восстановление после контакт-клавиатуры). Технически reply-меню требует
  сообщения-носителя; это НЕ спам после каждого шага.
- ОСТАЛОСЬ: railway up + live-проверка 4 правок.

## СЛЕДУЮЩИЙ ШАГ (не срочно, follow-up)
- Открыть Расширения→Apps Script, посмотреть doLookup_ (что возвращает как row) и doUpdateRow_
  (как использует row). Фикс: (а) doUpdateRow_ — не писать, если row > lastRow (guard); (б)
  выровнять номер строки, который возвращает doLookup_ (index vs row, +1 header). После фикса —
  почистить строки 207-236 (бэкап уже есть) и проверить: /start незнакомца не создаёт строку.

## Прогресс
- [x] Контекст, код, корень багов 1/4.
- [x] Live-осмотр Railway (deploy=CLI), env.
- [x] @bro_hit_bot изучен.
- [x] Целевая таблица прочитана (форма, 90 анкет).
- [x] Баги 1,2,4 — код готов + смоук-проверка.
- [x] Деплой-блокер зафиксирован, спрошен Игорь.
- [x] Маппинг бага 3 подготовлен, ждёт ОК.
- [ ] Деплой (по выбору Игоря) + live-проверка 1/2/4 в Telegram.
- [ ] Баг 3: Apps Script под 1Hoqi + live-проверка записи.

## Батч 4 (2026-07-04, ~23:30 МСК) — анимация / иконки / Мои отклики
СДЕЛАНО (код в /Users/.../ugc_bot_v2):
- start.py: _run_lookup_animation → стильная «радар»-анимация (спиннер ◜◝◞◟ + прогресс-бар ▰▱, тема UGC RADAR). Help-текст обновлён под новые иконки.
- keyboards.py: новые иконки меню (🧾 Моя анкета, 🎯 Запросы брендов, 📨 Мои отклики, 💬 Помощь, 🛠 Админка). Добавлена BTN_MY_APPS + кнопка в main_menu (2 ряда). ВАЖНО: картинки в кнопках Telegram невозможны — только текст+эмодзи (у brohit так же).
- sheets.py: dataclass Application + метод my_applications(chat_id) → action=my_applications.
- brands.py: хэндлер show_my_applications (BTN_MY_APPS) → список откликов со статусом (🕐 На рассмотрении / ❌ Отказ / 🎉 Оффер!).
- registration.py, profile.py: BTN_MY_APPS добавлена в _MENU_BTNS (исключения ввода).
- Смоук: py_compile всех файлов OK.

APPS SCRIPT (Код.gs, задеплоено = Версия 4 от 4 июл 2026, URL/ID те же):
- Роутинг: if (action === 'my_applications') return doMyApps_(data);
- doApply_: заголовок + 'Статус', новые отклики пишутся со статусом 'на рассмотрении' по умолчанию.
- doMyApps_(data): читает лист «Отклики» по именам заголовков (устойчиво к порядку колонок), фильтр по Chat ID, возвращает {brand_title, status, date}.

ЛИСТ «Отклики»: добавлена колонка G «Статус». G2:G1000 — выпадающий список [на рассмотрении, отказ, оффер] (data validation). Существующие 2 строки = 'на рассмотрении'.

ЛОГИКА СТАТУСА (вариант): источник правды — колонка «Статус» в листе «Отклики». Новый отклик = 'на рассмотрении'. Админ меняет статус в таблице через выпадашку (отказ/оффер). Бот читает и показывает креатору. (Расширение на будущее: смена статуса из админки бота + уведомление креатору при изменении.)

ПРОВЕРЕНО СЕРВЕРОМ: temp-функция вызвала doMyApps_({chat_id:357892821}) → вернула [{TastyBox Доставка, на рассмотрении, 04.07.2026},{FitFuel Спортпит, ...}]. Скобки сбалансированы, роутинг/статус на местах. Temp-функция и служебная ячейка I1 удалены (cleanup). Версия 4 temp-код НЕ содержит.
НЕ ПРОВЕРЕНО ГЛАЗАМИ В БОТЕ: требуется `railway up` (Игорь) — после деплоя нажать 📨 Мои отклики в @ugc_radarbot.

ОСТАЛОСЬ: Игорю выполнить railway up для бота. Apps Script уже live.

## Батч 5 (2026-07-05) — фикс админки + анимация + скорость
- БАГ АДМИНКИ (корень): admin.py матчил кнопку захардкоженной строкой "⚙️ Админка", а иконку сменили на "🛠 Админка" → не срабатывало. Фикс: F.text == BTN_ADMIN (константа). Проверил grep — других захардкоженных старых кнопок нет.
- Анимация: текст "Сканирую радар креаторов" → "Ищу тебя в базе". Сделана ЦИКЛИЧНОЙ и ПАРАЛЛЕЛЬНОЙ поиску: lookup запускается asyncio.create_task, полоса ▰▱ крутится/перезаполняется пока идёт запрос, обрывается по ответу. Предохранитель 40 кадров (~14с). Убрал «мёртвую» паузу на 100%.
- Скорость: sheets.brands() получил TTL-кэш 30с. Листание карточек/отклик теперь бьют по webhook 1 раз вместо 2-3. Корень абсолютной задержки = round-trip к Apps Script (cold start + чтение листа), ~1-3с — при желании оптимизировать doLookup_/doStats_ (читать только нужные колонки).
- Смоук: py_compile OK, чек-ассерты OK.
- ОСТАЛОСЬ: railway up (Игорь). Проверить в боте: 🛠 Админка открывается, анимация /start циклична.

## Батч 6 (2026-07-05) — текст отклика / ускорение / автоскролл
- brands.py: текст popup отклика → "Отклик отправлен! За статусом следи в меню «📨 Мои отклики»." + call.answer вынесен НАВЕРХ (ответ мгновенно, запись в таблицу после).
- chat_cleanup.render_screen: добавлена микро-пауза 0.12с между send нового экрана и delete старого → чинит «иногда не скроллит» (гонка send/delete, особенно на карточках брендов).
- ЗАМЕР СКОРОСТИ (сервер, temp _tmpBench, avg 5, warm): doLookup_=357мс, doStats_=88мс, doBrands_=85мс. Вывод: чтение таблицы НЕ узкое место. Осн. переменная — редкий cold start Apps Script (2-4с после простоя) + сеть. Column-limiting дал бы ~100-200мс — низкий ROI, не трогал. Keep-warm триггер не гарантирует прогрев web-app пути → не добавлял (не обещать без цифры). Temp _tmpBench и I1 убраны.
- Уже ускорено кодом (этот+прошлый батч): кэш брендов 30с (2-3 запроса→1), answer-first отклик (мгновенный popup), анимация параллельно поиску.
- Смоук: py_compile OK.
- ОСТАЛОСЬ: railway up (Игорь). Проверить: текст отклика, автоскролл на брендах, 🛠 Админка.

## Батч 7 (2026-07-05) — комьюнити-фичи T1 + трек-рекорд
Код бота (в папке проекта, компилируется):
- Персональная лента брендов: brands.py _personalize() — бренды с совпадением категории креатора вверх + тег 🔥; keyboards.brands_list_keyboard(brands, hot_ids). show_brands/back_to_list фетчат креатора и персонализируют.
- Причина отказа: sheets.Application.reason; в «Мои отклики» при статусе отказ показывается «Причина: …».
- Трек-рекорд/уровень: profile._track_record_line() — «Откликов N · Офферов M · Уровень Новичок/Про/Топ» из my_applications; показывается в «Моя анкета» (устойчиво к ошибке фетча).

Apps Script (задеплоено = Версия 5 от 5 июл 10:13, URL/ID те же):
- doMyApps_ теперь отдаёт reason (колонка 'причина').
- onOtklikiEdit(e): при смене «Статус» на оффер/отказ шлёт креатору сообщение в Telegram напрямую (UrlFetchApp → api.telegram.org/bot<token>/sendMessage). Токен читается из Script Properties 'BOT_TOKEN' (значение НЕ в коде). Если токена нет — no-op.
- setupStatusPush(): ставит installable onEdit-триггер. Запускать Игорю один раз (нужно согласие на новые разрешения UrlFetchApp/ScriptApp — это его действие, не агента).

Лист «Отклики»: добавлена колонка H «Причина».

ОСТАЛОСЬ (пуш само-работающий) — шаги Игоря:
1) railway up (бот-часть T1+трек-рекорд).
2) Для пушей: Project Settings → Script Properties → добавить BOT_TOKEN = токен @ugc_radarbot; затем в редакторе Apps Script выбрать функцию setupStatusPush → Выполнить → согласиться на доступ. После этого смена статуса в «Отклики» шлёт креатору пуш.

Далее: T2 ставки/дайджест (могу делать). T3 (реферал/канал/контент) — нужны решения Игоря.

## Диагностика пушей (2026-07-05) — не приходят уведомления
Проверено на реальных местах (не по коду):
- Executions: setupStatusPush установил триггер (11:20:34 OK, до этого 11:20:11 Сбой). onOtklikiEdit СРАБАТЫВАЕТ на смену статуса (11:20:53/11:21:14/11:27:23/11:30:47), «завершено», ~1-1.5с → доходит до UrlFetchApp (guards, включая token, пройдены).
- Telegram Игоря: бот пишет нормально (карточка анкеты с трек-рекордом «Откликов:4 · Офферов:2 · Уровень:⭐Про» — T2 работает live!). НО пушей о статусе НЕТ.
ВЫВОД: токен в Script Properties BOT_TOKEN неверный/с мусором. Railway-бот шлёт в тот же chat 357892821 успешно (его токен рабочий), Apps Script со своим токеном — Telegram молча отклоняет (muteHttpExceptions). Правка кода onOtklikiEdit чистая, диагностика убрана, I1:K1 очищены.
FIX Игорю: заново скопировать ТОЧНЫЙ токен из Railway Variables BOT_TOKEN (или BotFather) в Script Properties BOT_TOKEN, без пробелов/переносов. Проверка: открыть https://api.telegram.org/bot<TOKEN>/getMe → должно быть ok:true. Затем сменить статус в Отклики → пуш придёт.
ПОБОЧНО: замечен рассинхрон — alert отклика показал старый текст «М魏 свяжемся...» (возможно старая незакрытая модалка либо не последний railway up). Проверить после.

## Батч 8 (2026-07-06) — фикс удалённых колонок + живая «Моя анкета»
ПРИЧИНА (найдено на реальной таблице): Игорь удалил 2 столбца между «инстаграм» и «оплата». Код Apps Script читал/писал по жёстким номерам → «Оплата/Категория/Портфолио/Есть в боте/Chat ID» съехали на 2 → регистрация писала в чужие ячейки, lookup читал не то → «Моя анкета» пустая.
ТЕКУЩАЯ РАСКЛАДКА КОЛОНОК (Ответы на форму (1)): A Время, B Имя, C Возраст, D Телефон, E Телеграм, F Адрес, G Инстаграм, H Рост, I Размер, J Статус в базу, K Актёрские, L Фото, M Параметры тела, N Оплата, O Др.соцсети, P Категория, Q Портфолио, R Источник, S Есть в боте, T Chat ID, U Обновлено.
ФИКС (по решению Игоря — header-based + живая анкета):
- Apps Script: добавил doProfile_(data) — чтение строки по Chat ID, поля по ЗАГОЛОВКАМ (idx по подстроке). Роут action=profile. Переписал блок регистрации на header-based (hidx/put) — пишет каждое поле в колонку по заголовку. Задеплоено = Версия 6 (6 июл 00:19), URL/ID те же. Save на СВЕЖЕЙ вкладке заработал (прошлые сбои были из-за побитого черновика — Игорь сбросил через F5+Покинуть).
- Бот: sheets.profile(chat_id) → action=profile. profile.show_profile теперь тянет ЖИВЬЁМ из таблицы (fallback на локальный кэш). _profile_text_from_sheet.
ОСТАЛОСЬ: railway up (Игорь) — включит живую «Мою анкету». Push — отдельный незакрытый вопрос (после сброса черновика перепроверить). doLookup_ пока по старым номерам (дедуп на /start работает по имени/телеграму A-G, не сломано; данные для локального кэша могут быть сдвинуты — вторично, т.к. анкета теперь живая).
Смоук бота: py_compile OK.

## Батч 9 (2026-07-06) — миграция на Postgres + фоновый синк (скорость/надёжность)
Решение Игоря: постоянная БД (Railway Postgres) + синк с таблицей. Он добавил Postgres в проект (сервис есть). НЕ менять DATABASE_URL пока синк не готов (флипнем одним деплоем).
СДЕЛАНО (бот, компилируется):
- config._normalize_db_url: postgres:// → postgresql+asyncpg://, sqlite:// → sqlite+aiosqlite://, убирает ?sslmode. Бот работает и на SQLite, и на Postgres без правок.
- database.py: async init_db (create_all через run_sync — оба движка), pool_pre_ping. main.py: await init_db().
- requirements: +asyncpg==0.30.0.
- models: CachedBrand, CachedApplication (+ synced_at у Creator).
- sync.py: sync_brands (таблица→БД, полная замена), get_cached_brands, cache_applications/get_cached_applications, run_sync_loop(120с). main.py стартует run_sync_loop фоновой задачей.
- Чтения переключены на БД: brands._fetch_brands → get_cached_brands (фолбэк в таблицу при холодном кэше); Мои отклики → stale-while-revalidate (мгновенно кэш, потом тихо свежие); трек-рекорд в «Моя анкета» → из кэша (убрал лишний webhook).
- Ранее в этом заходе: ретрай в sheets._post (первый тап греет), ⏳ индикатор загрузки в Моя анкета/Мои отклики/Бренды.
ОСТАЛОСЬ по БД: bulk-синк креаторов (профиль целиком) — сейчас профиль ещё webhook+кэш-в-Creator; можно добить. Потом флип DATABASE_URL + railway up.
ОСТАЛОСЬ вообще: пуш (перепроверить на чистом проекте), карточка вакансии (фото/ссылки/условия + колонки в лист Бренды).

## Батч 10 (2026-07-06) — фикс записи в неверные колонки (Igor нашёл)
ПРИЧИНА: header-based я сделал только в РЕГИСТРАЦИИ (appendRow), но doUpdateRow_ (срабатывает на /start recognition + обновление) остался с фиксированными getRange(row,21/22/23) = писал 'да'/chat_id/дата в U/V/W вместо S/T/U. Из-за этого chat_id уходил не в ту колонку → doProfile_ не находил строку по Chat ID → пустая анкета.
ФИКС: в doUpdateRow_ заменил getRange(row,21/22/23) на getRange(row,_hcol('есть в боте'/'chat id'/'обновлено', fallback)) + добавил хелпер _hcol (поиск колонки по заголовку). Теперь и регистрация, и обновление пишут по заголовкам → S(Есть в боте)/T(Chat ID)/U(Обновлено). Задеплоено = Версия 7 (6 июл 19:54). Apps Script only, действует сразу.
ОСТАЛОСЬ: junk в старых строках (chat_id продублирован в V, дата в W, 'да' в U вместо даты) — не ломает чтение (по заголовкам), но можно почистить V:W и восстановить U. Бот-часть (гибрид/скорость) ждёт railway up от Игоря.

## Батч 11 (2026-07-06) — always-react + фон-отклик + bulk-синк + крупная анимация
1) КНОПКИ ВСЕГДА РЕАГИРУЮТ: у show_profile убрал StateFilter(None) + state.clear() — «Моя анкета» больше не молчит при застревании в состоянии (раньше лечилось /start).
2) ФОНОВЫЙ ОТКЛИК: brand_apply теперь add_cached_application (мгновенно в БД) + запись в таблицу и уведомление админов в фоне (create_task). Таймаут таблицы не теряет отклик и не блокирует юзера. Причина прошлого WARNING «brand apply record failed: Timeout» — снята.
3) BULK-СИНК: sheets.all_creators()/all_applications() + sync.sync_creators()/sync_applications() в run_sync_loop (каждые 120с, вместе с brands). Профиль/отклики теперь всегда из БД → мгновенно даже на первом тапе. Apps Script: doAllCreators_/doAllApplications_ + роуты all_creators/all_applications. Задеплоено = Версия 8 (6 июл 20:52).
4) АНИМАЦИЯ: /start-анимация крупнее — тематический эмодзи + широкий бар (10 сегм.) с процентами.
Проверено: py_compile + импорт-смоук + функц.тест БД-слоя (SQLite) OK. Осталось: railway up (Игорь). Для «прям стикер как у брохит» — попросить у Игоря file_id стикера.

## Батч 12 (2026-07-09, ночь) — clasp, порядок колонок, фото, дедуп-кнопки, пуш→бот
Сессия автономная (Игорь спал). Настроен **clasp** (node+clasp+Google-login Игоря) →
Apps Script правится/деплоится из терминала. Скрипт склонирован в `apps_script/`
(в .gitignore — там SECRET). Деплой вебхука: `clasp create-deployment -i <DEPLOY_ID>`,
DEPLOY_ID = AKfycbyDWD…1saGUY (тот же URL). Текущая версия Apps Script = **v14**.

СДЕЛАНО:
- **Автоскролл /start**: ветки «не найден»/«подтверди» переведены на render_screen (было
  edit-in-place). + убран forget_screen в cmd_start → старый экран «Готово» больше не
  повисает после /start. Проверено ГЛАЗАМИ в Telegram Desktop (весь флоу скроллит).
- **Анимация загрузки**: анимированный custom-emoji из пака LoadingStatusByTimDesign
  (get_sticker_set → custom_emoji entity), фолбэк на текст. Требует Premium у владельца
  бота (есть). render_screen получил параметр entities.
- **Дедуп-кнопки**: /start при узнавании показывает Имя/Telegram/Instagram + «✅ Да, это я!» /
  «❌ Это не я» (обе ветки уверенности унифицированы). «Да» → update_row (S=да, T=chat_id).
  Убран попап-тост. «Обновить данные» — только в «Моей анкете».
- **Порядок колонок (ОКР Игоря)**: НАЙДЕН настоящий баг — doUpdateRow_ писал по
  ЗАХАРДКОЖЕННЫМ сдвинутым (+2) номерам (other_socials→Портфолио и т.д.). Переведён на
  header-based. getAllDataRows_ тоже (чинит doStats_ inBot). Заголовок G переименован в
  «Instagram». Матчинг instagram устойчив к латинице/кириллице (_needleHit_ 'a|b').
  Решение структуры: G=Instagram, O=Другие соцсети (уже была, пустая), Q=Портфолио.
- **Поле «фото»** (колонка L): шаг регистрации с кнопкой «⏭ Добавить позже», показ в
  анкете, правка, модель Creator.photo + идемпотентная миграция БД (ALTER ADD COLUMN).
  Текст: «…не менее 4 фото. Это нужно для добавления в базу креаторов: t.me/ugc_creatory».
- **ПУШ при смене статуса → перенесён в БОТ** (sync.py). Apps Script onEdit-пуш был хрупок
  (авторизация UrlFetchApp/токен) → «не приходил». Теперь бот в sync_applications ловит
  переход статуса (→оффер/отказ) сравнением с кэшем и шлёт пуш напрямую по токену.
  onOtklikiEdit-триггер УДАЛЁН (чтобы не было дабл-пуша). Проверено live: пуш доставлен.

ВАЖНЫЙ ИНЦИДЕНТ (исправлен): первая версия бот-пуша слала пуш на КАЖДЫЙ синк (дубли строк
(chat_id,brand_title) ломали сравнение) → реальные креаторы (357892821 Игорь, 810491328)
спамились ~20 мин каждые 2 мин. ФИКС: схлопывание дублей в один эффективный статус
(финальный приоритетнее) до сравнения → пуш строго на реальный переход, 1 раз. Юнит-тест
+ live: спам прекратился, синки без пушей на неизменных данных.

Бэкап таблицы перед правками: лист-копия «BACKUP UGC RADAR … 2026-07-09».
Тест-аккаунт @packman_production (5481126169, «Тест Радар») — зарегистрирован для проверки,
после — удалён через временный cleanup_test (все служебные webhook-экшены убраны в v14).

ХВОСТ (не критично): фоновая запись отклика (_record_apply_bg через create_task) может
потеряться, если контейнер редеплоится в момент записи (отклик остаётся в БД-кэше, но
вытирается следующим синком). В норме работает. Кандидат на await/надёжную очередь.

## Батч 13 (2026-07-09) — НАКОНЕЦ надёжный автоскролл + имя бренда в пуше
- **АВТОСКРОЛЛ (корень найден и починен)**: любое удаление старого экрана в один такт
  с отправкой нового (send→delete ИЛИ delete→send) уводило ленту от нового сообщения на
  части клиентов (у Игоря-админа — стабильно). ФИКС в chat_cleanup.render_screen:
  отправляем новый экран ПЕРВЫМ (юзер внизу → клиент проматывает к входящему), а прошлый
  экран + эхо-ответ юзера удаляем ОТЛОЖЕННО и в ФОНЕ (asyncio.create_task, sleep 0.4с) —
  вне пути прокрутки. Проверено ГЛАЗАМИ на аккаунте Игоря 3 раза (start→дедуп,
  «Да это я»→узнал, «Моя анкета»→профиль) — всегда виден последний экран, «↓» нет.
- **Имя бренда в пуше**: старые отклики хранили служебный id («br2») → пуш показывал «br2».
  Новые отклики (brand_apply) уже пишут brand.title. Добавлен резолв sync._brand_display_name:
  id→название из кэша брендов; если это id вида brN без названия — формулировка «по твоему
  отклику» (без id). 
- НАБЛЮДЕНИЕ (в бэклог): Игорь-АДМИН получает уведомления об откликах в тот же чат с ботом
  (raw send, не через render_screen) → они не чистятся и засоряют ленту у админов. У обычных
  юзеров этого нет. Кандидат: слать админ-уведомления отдельным каналом/не в бота.

## Батч 14 (2026-07-09 утро) — автоскролл ПО-НАСТОЯЩЕМУ + анимация загрузки везде
КОРЕНЬ (наконец точно): удаление любого сообщения у нижнего края в один такт с показом
нового экрана дёргало ленту → «слетевший автоскролл». РАЗДЕЛИЛ два пути в render_screen:
- delete_trigger is None (нажата инлайн-кнопка / продолжение без ввода юзера): текущий
  экран уже у самого низа → РЕДАКТИРУЕМ его на месте (edit_message_text). Лента не
  двигается вообще — скроллу неоткуда слетать. Чат чистый (одно сообщение).
- delete_trigger задан (юзер ввёл текст → эхо появилось НИЖЕ экрана): ШЛЁМ новое (оно
  ниже эха = самое нижнее → клиент проматывает), прошлый экран (≥2 от низа) удаляем в
  фоне; эхо не трогаем.
Плюс reply-меню (ReplyKeyboardMarkup) всегда через send (edit его не принимает).
_finish_recognized теперь показывает экран СРАЗУ (правка на месте), запись в таблицу/БД —
в фоне (create_task), чтобы «узнал тебя» не ждал вебхук.
ПРОВЕРЕНО ГЛАЗАМИ на аккаунте Игоря (новый деплой): /start→«Нашёл тебя», «Да это я»→
«узнал тебя», «Моя анкета»→профиль — ВСЕ три внизу, «↓» нет. Кейс «да это я» (жалоба) чинен.

АНИМАЦИЯ ЗАГРУЗКИ ВЕЗДЕ: helper chat_cleanup.render_loading (анимированный custom-emoji
из пака, фолбэк ⏳). Используется в /start (Ищу тебя), submit (Сохраняю анкету),
холодном кэше «Моя анкета»/«Мои отклики». На кэш-хитах загрузку НЕ показываем (мгновенно,
чтобы не мигало). _get_loading_emoji/LOADING_EMOJI_SET перенесены в chat_cleanup (общие),
дубли из start.py убраны.

КЛАТТЕР при тестах — артефакт рестартов (in-memory _LAST_SCREEN сбрасывается, прошлые
экраны не удаляются). В проде без рестартов каждый переход держит один экран.

## Батч 15 (2026-07-09) — запрет дублей откликов + починка уведомлений
ЖАЛОБА: «уведомления об откликах не приходят» + нужен запрет повторного отклика.
КОРЕНЬ (найден): 1) doAllApplications_ матчил «бренд» подстрокой и цеплял «Бренд ID» →
в кэш/пуши шёл «br2» вместо названия; 2) ДУБЛИ откликов (тесты Игоря: br2 x2, br3 x2)
— мой антиспам их «схлопывал», из-за чего смена статуса у дублированного отклика НЕ
детектилась → пуш не слался. Т.е. запрет дублей напрямую чинит уведомления.
СДЕЛАНО:
- Apps Script doApply_: ЗАПРЕТ ДУБЛЯ по (Chat ID + Бренд ID). Возвращает
  {ok:true, duplicate:true, status, reason} если отклик уже есть, иначе создаёт.
- doAllApplications_: читает «Бренд» (название) через exact-match, не «Бренд ID».
- Guard: незнакомый action больше НЕ проваливается в регистрацию (плодил мусорные
  строки креаторов). Возвращает {ok:false, error:'unknown action'}.
- Бот sheets.apply → возвращает payload. brand_apply переписан: СИНХРОННО (под
  loading_guard), при дубле показывает «Ты уже откликался… Текущий статус: … Причина: …»,
  при успехе — кэш + СИНХРОННОЕ уведомление админов + экран успеха. Убран _record_apply_bg.
ПРОВЕРЕНО (webhook + логи): дедуп (2-й apply → duplicate+статус); названия брендов
верные (PapKids/NewOrganic, не br2); статус-пуш ДЕТЕКТИТСЯ и шлётся («status pushes
sent: 1» на переходе на рассмотрении→оффер).
ЧИСТКА: удалены тестовые дубли откликов Игоря (12 шт) + тестовый мусор. Все служебные
webhook-экшены убраны, Apps Script = v21 (чистый). ПРЕД-СУЩЕСТВУЮЩЕЕ (не трогал):
дубликат РЕАЛЬНОГО креатора «Анастасия Федосс» (810491328 дважды) — кандидат на дедуп.

## Батч 16 (2026-07-09) — строгий дедуп креатора + восстановление данных + оформление анкеты
ЖАЛОБА: «Дарья Макеева» подтянулась к строке 131 «Дарья Еремеева» (общее только имя
«Дарья») и перезаписала её. Плюс просьба причесать «Мою анкету».
КОРЕНЬ: doLookup_ матчил по нечёткому совпадению ИМЕНИ (levenshtein) без обязательного
телеграма. На сабмите регистрации это ложное совпадение шло в update_row → перезапись
чужой строки.
СДЕЛАНО (процесс):
- doLookup_: СТРОГО — узнаём только если совпал телеграм-хендл (уникальный) И имя
  (sim≥0.5). Без хендла не матчим. Всегда needsConfirmation (инстаграм на подтверждение).
- registration submit: убран нечёткий lookup-fallback перед записью. Нет привязанной
  строки (не был узнан+подтверждён) → ДОБАВЛЯЕМ новую строку. Проверено webhook:
  чужой хендл + «Дарья Еремеева» → not found; реальный @Darya_nefedyeva → найден.
- САМОЛЕЧЕНИЕ sheet_row: doAllCreators_ отдаёт row, sync_creators держит Creator.sheet_row
  в актуале → правки анкеты всегда в свою строку (даже если строки сдвинулись/перенесли).
СДЕЛАНО (данные): временный экшен fix_makeeva открыл БЭКАП (openById), восстановил
Дарью Еремееву в строку 131 и добавил Дарью Макееву новой строкой 210 (её chat 676848432).
Служебный экшен убран (Apps Script v24, чистый).
СДЕЛАНО (оформление): «Моя анкета» — эмодзи-метки по полям + разбивка на блоки +
_clean_links обрезает трекинговые хвосты ссылок (?igsh=…&utm_source=… → чистый url).

## Батч 17 (2026-07-09) — привязка креатора по chat_id (а не по номеру строки)
ЖАЛОБА: после удаления строки Макеевой (210) бот давал «левые данные» и логи сыпали
«register/update failed: row out of range» (её DB.sheet_row=210, а строку удалили →
номер вне диапазона).
КОРЕНЬ: запись/правка шли по НОМЕРУ строки (sheet_row) — он хрупок: при удалении/сдвиге
строк указывает не туда или вне диапазона.
ФИКС:
- Apps Script doUpdateRow_: строку ищем по Chat ID (стабильно), переданный номер —
  только fallback для ПЕРВОГО узнавания (chat_id ещё не в строке) И только если валиден.
  Не нашли валидной строки → {updated:false, notfound:true}, чужую строку НЕ трогаем.
  Проверено webhook: несуществующий chat+row99999 → ничего не пишет; update по chat Игоря
  (row=1) → нашёл его 206.
- registration submit: при updated:false → ДОБАВЛЯЕМ новую строку (расстрянет креатора,
  у которого удалили строку). sheet_row=None → самоправится на синке.
- «Моя анкета» _refresh_profile_hybrid: если креатора нет в таблице (profile=None) —
  не показываем устаревший кэш, зовём /start.
ИТОГ по Макеевой: её след. /start+регистрация теперь ДОБАВИТ её чисто (append), «Моя
анкета» не покажет чужие данные. Данные из удалённой строки 210 в таблице не сохранились
(удалены Игорём) — восстановятся при её повторной регистрации.

## Батч 18 (2026-07-11) — воронка CJM (заходы/регистрации/уникальные отклики) + фикс age
ЗАПРОС: аналитика — сколько нажали /start (вкл. незарегавшихся) + их список; в админке
воронка: уникальные заходы / регистрации / уникальные отклики.
СДЕЛАНО:
- Модель BotVisit (новая таблица bot_visits — create_all создаёт сам, миграция не нужна):
  каждый уникальный /start (дедуп по tg_id), first_seen/last_seen.
- record_visit() в cmd_start — неблокирующе (create_task).
- funnel_stats(): visits / registrations (креаторы в боте) / unique_applicants (РАЗНЫЕ
  chat_id в откликах). Админ «Аналитика» показывает воронку. Новая кнопка «Экспорт
  заходов» → zahody.xlsx (список с username/имя/время).
ПОБОЧНО (нашёл в логах и починил): sync_creators падал с value too long for varchar(16)
— колонка Creator.age. Кто-то ввёл длинный «Возраст». Расширил age → TEXT + миграция
ALTER COLUMN TYPE. После фикса синк ожил: 6 → 22 креатора (раньше был застрявший кэш).
ЗАМЕЧАНИЕ: bot_visits начинает считать С ЭТОГО деплоя — заходы копятся вперёд; кто
нажимал /start раньше, попадёт в счётчик при следующем /start.

## Батч 19 (2026-07-12) — гейт «нельзя откликнуться без регистрации» (на стороне таблицы)
ЖАЛОБА: незарегистрированный (@akovalchuk_pmu, chat 7331713990) попал в «Отклики» на
PapKids — нажал /start и откликнулся, минуя анкету. Первый фикс (DB-гейт
get_creator_by_tg_id) НЕ сработал: запись Creator в БД остаётся после удаления строки из
таблицы → фальшивый пропуск.
ФИКС (источник правды — таблица): doApply_ в начале проверяет _isRegistered_(chat_id) —
есть ли chat_id в листе анкет. Нет → {ok:true, not_registered:true}, отклик НЕ создаётся.
Бот brand_apply: DB-гейт убран; на not_registered показывает «Чтобы откликаться, заполни
анкету — /start». Просмотр брендов остаётся свободным.
ПРОВЕРЕНО (webhook, ровно логика бота): незарег (999888777 и реальный 7331713990) →
not_registered, число откликов не меняется (14→14); зарегистрированный Игорь → проходит.
Живьём в Telegram блок не гонял — активен аккаунт Игоря (зарег), незарег-аккаунт не
трогал, чтобы не лезть в его сессию; но вебхук бьёт ровно ту же серверную функцию.
ХВОСТ: старый «осиротевший» отклик @akovalchuk_pmu (до фикса) всё ещё в листе «Отклики» —
Игорь решит: оставить как лид или удалить.

## Батч 20 (2026-07-30) — 2-я волна + донабор «UGC-пак», чистка заблокировавших
СДЕЛАНО: пайплайн 2-й волны «Сингл» (оффер 15 сдавшим ролик → дата в кол. Q, пинки +1ч/+6ч,
напоминание за день) и донабор по всей базе (UGC-пак за ролик к 03.08, 373 доставлено:
38 «взял», 16 «не интересно» → колонка «Донабор 3.08» в листе креаторов).
Согласование по полу — общий guess_gender из bot/cards.py (добавлены имена на -ь: Игорь/Любовь).
УДАЛЕНЫ 23 заблокировавших бота (+ фантом 888000111) из обоих листов и всех таблиц БД.
Бэкап их строк — лист «УДАЛЕНЫ заблокировали бота 30.07».

ГРАБЛИ (стоили часа): **при активном ФИЛЬТРЕ на листе `sh.deleteRow()` молча не удаляет
строки, скрытые фильтром** — Apps Script возвращает успех, лист не меняется, исключения нет.
Диагностика: сравнить getLastRow() до и после внутри самой функции. Решение в doRowsDel_:
снять фильтр (запомнив range + критерии по колонкам через getColumnFilterCriteria().copy()),
удалить строки, восстановить фильтр. То же может касаться любых массовых правок структуры.
