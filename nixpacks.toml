# ffmpeg/ffprobe нужны боту для нормализации работ-видео перед постингом в канал
# (обложка + размеры, см. docs/CHANNEL_POSTING.md). aptPkgs добавляется к авто-сборке,
# start-команда по-прежнему детектится автоматически.
[phases.setup]
aptPkgs = ["ffmpeg"]
